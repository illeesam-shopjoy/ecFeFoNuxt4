node_modules
*.log*
.nuxt
.nitro
.cache
.output
dist

# 환경변수 파일 — 2026-09-15 사용자 지시로 .env/.env.local/.env.development/.env.production
# 전부 커밋 허용으로 전환(이전엔 여기서 제외했었음). 안에 실키(토스페이먼츠 시크릿 등)가
# 들어있고 이 저장소는 Public이라는 점을 사용자에게 알리고 명시적으로 진행 확인받았음.
# scripts/deploy-sites.mjs 전용 비밀(Netlify 토큰/Gmail 앱 비밀번호)만 계속 제외 — .env.deploy.example 만 커밋
.env.deploy

# 추가 ###########
*.bak
*@*
~*
# *lock.json
* copy.*
* 복사본.*
# Local Netlify folder
.netlify

# npm 잔재 (이 프로젝트는 pnpm 전용, package.json packageManager 참조)
package-lock.json
