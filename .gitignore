node_modules
*.log*
.nuxt
.nitro
.cache
.output
dist

# 환경변수 파일 (민감 정보 포함 가능)
# .env.development 는 커밋 허용 (민감 정보 없음)
.env
.env.local
.env.development
.env.production
# scripts/deploy-sites.mjs 전용 비밀(Netlify 토큰/Gmail 앱 비밀번호) — .env.deploy.example 만 커밋
.env.deploy

# 추가 ###########
*.bak
*@*
~*
# *lock.json
* copy.*
* 복사본.*
# Local Netlify folder
.netlify

# npm 잔재 (이 프로젝트는 pnpm 전용, package.json packageManager 참조)
package-lock.json
