/** 지도 SDK 공개키(ecBeBo MapKeysRes) — 브라우저에 노출 가능한 키만. 값이 비어 있으면 미설정. */
export interface SyMapKeysType {
  kakaoMapJsKey?: string; // 카카오맵 JavaScript 키
  naverMapClientId?: string; // 네이버 클라우드 맵 Client ID
}
